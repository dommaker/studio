# @dommaker/studio-notification

Notification service for agent-studio (JSONL file storage).

## Exports

| Export | Description |
|--------|-------------|
| `NotificationService` | Notification service class (create / query / mark, FileStore-backed) |
| `notificationService` | Shared singleton instance |
| `CreateNotificationInput` | Input type for creating notifications |
