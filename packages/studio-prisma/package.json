{
  "name": "@dommaker/studio-prisma",
  "version": "0.1.0",
  "private": true,
  "description": "Database schema for Agent Studio",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@dommaker/harness": "file:../../../harness/dommaker-harness-0.15.0.tgz",
    "@prisma/client": "^5.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.7.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
