generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// ── All models migrated to FileStore (Spec 1-4) ──
// Schema preserved for Phase 4 removal
