# packages/studio-shared/src

### 职责

本目录是 Agent-Studio 的前后端共享库，提供 CLI 框架、配置管理、常量定义、事件总线与文件存储等通用基础设施，为 apps/api 等多个上层模块提供复用的工具与类型。

### 核心导出

| 导出 | 文件 | 说明 |
| --- | --- | --- |
| `Parser`, `ParsedArgs` | cli/parser | 命令行参数解析，支持短参数、长参数、JSON 等 |
| `formatOutput`, `Format` | cli/formatter | 输出格式化 (table/json/csv) |
| `loadConfig`, `getConfig`, `StudioConfig` | cli/config | CLI 配置文件 (.studio/config.yaml) 加载与访问 |
| `registerCommand`, `getCommand`, `runCommand`, `Command` | cli/command | 命令注册与执行框架 |
| `formatError`, `createCliError`, `CliError`, `ERROR_CODES` | cli/error | 统一错误处理与格式化为字符串 |
| `loadConfigEnv`, `AgentStudioConfig` | config | 系统级配置加载 (~/.studio/config.env) 及类型定义 |
| `LEVEL_CONFIG`, `getLevelConfig`, `getLevelSalary` 等 | constants/levels | 全局统一的职级配置与辅助函数 |
| `eventBus`, `StudioEventBus` | event-bus | 内存事件总线，支持通配符订阅；监听器上限显式抬到 64（`workunit.status_changed` 模块级订阅方常态 11+ 每个 AgentLoop 实例再 +1，属业务扇出非泄漏，P9） |
| `AgentProfileData`, `RuntimeStateData`, `ChannelData`, `ChannelMessageData` 等 | file-store | 文件存储基础数据类型。`ChannelData.routing`（#466）= 频道级「阶段→角色」路由表 `{ plan?, implement?, review? }`（profile id，档空 = 回池涌现；`defaultPipeline` 已吞并退役，存量由 api 侧 migrate-routing 迁移），解析/校验语义属主在 api channels/routing.ts |
| `EvolutionProposalData`（constraintChange: message/exception/new-entry/retire——retire 为 #82 D6 退役落点） | file-store-types | E1 进化提案类型 |
| `resolveVpsWorkspace`, `resolveWorkspacesDir` | vps-workspace（仅 /node 入口） | 'VPS' 工作区命名约定与 ~/.studio/workspaces 扫描的唯一属主（2026-08 起；#481 后消费方 = local-workspace 启动复用与 GET /workspaces/runtimes 本机 CLI 清单，记录的 workspaceRoot 已退出执行面，禁止第三处手扫） |
| `studioDir`, `studioPath`, `defaultStudioDir`, `warnIfNonProdUsesProdRoot` | config/studio-dir（`./studio-dir` 子路径入口） | 数据根解析单入口（issue #89）：STUDIO_HOME 优先，缺省 ~/.studio；全部数据区读写必须经此，禁止新增 `os.homedir() + '.studio'` 硬编码 |
| `createSettledTracker` / `SettledTracker` | utils/settled-tracker.ts | #228 确定性等待原语（#158 先例抽取）：fire-and-forget 异步链（事件订阅消费 / best-effort 收尾）的在途登记 `track` + `waitForSettled` 等待（while 循环兜底级联），供测试替代盲等；消费方：pmo/progress-rollup、workunit.service（reviewPassed 收尾）、agents/loop/review-dispatcher、pmo/analysis-handoff、pmo/map-opening、pmo/spec-materialization（后两者 2026-08-26 补：测试删目录前等在途链落定，消 /tmp 复活竞态） |
| `setReadMetricsSink`, `runWithLoopLabel`, `readMetricsBegin`, `emitReadMetric` | read-metrics.ts | #323 读口量化测量 sink：模块级 sink 默认 null（关闭 = 读口一次 if 外零开销）；`runWithLoopLabel` 基于 AsyncLocalStorage 做循环归因（无 label → 'unlabeled'）；file-store 四读口（readJson/readJsonl/readIndexForQuery/readdirCached）内计时埋点（statMs/readParseMs/cloneMs/cacheHit），锁内裸读路径不埋 |
| `writeStudioEvent`, `readStudioEvents`, `parseStudioEventPayload`, `getStudioEventTime`, `resolveStudioEventsFile`, `isEmptyEventPayload`, `defaultStudioEventLevel` | studio-events.ts | #361 D18 事件唯一写口（自 apps/api utils 下沉）：StudioEvent envelope `{ type, source?, payload(JSON string), createdAt, level? }`；空 payload 拒收、永不抛出、knowledge:*/tool:call 缺省 debug 级；显式传 level（含 'info'）必落 envelope 字段（度量地基票 C：否则轮转分类回退 type 默认分级，提级失效——skill_used 即靠显式 info 提为 signal）；apps/api utils/studio-events.ts 是兼容薄壳，packages 直用本入口。消费方：studio-agent output-capture 5 emit、api 30+ 模块 |
| `isTestEnv`, `testTmpRoot`, `resolveStudioLogsDir`, `resolveStudioLogFile` | log-path.ts | #361 测试/生产日志路径隔离规则唯一属主（自 apps/api utils 下沉）：VITEST/NODE_ENV=test → os.tmpdir()/studio-test-logs，生产 → ~/.studio/logs；apps/api utils/studio-log-path.ts 为兼容薄壳 |
| `matchResolutionPatterns`, `isActionableMaturity`, `formatRkbHint` | resolutions.ts | #361 RKB 匹配核心（原 studio-agent 与 api resolution.service 逐字重复段收一）：regex(i) 失败回退小写子串包含、verified/canonical 成熟度闸门、RKB hint markdown 格式化；文档扫描与 fix 提取口径留在调用方。**#587 后消费方收敛为一个**：前两个由 apps/api `knowledge/resolution.service.ts` 独用（studio-agent 那份薄包装已摘），`formatRkbHint` 是那份薄包装的输出格式、**暂无生产调用方**（service 自拼另一种格式），去留待「两种 RKB 提示格式是否统一」另票裁 |
| `readJsonlTail`, `iterateJsonlLinesBackward` | jsonl-tail.ts | 通用 JSONL 尾部倒读原语（架构评审候选 2，泛化自 apps/api studio-events-tail）：「取尾部 N 行 / 倒扫早停 / 游标翻旧页」的 O(增量) 读口，替代「取尾部摊成全量 readJsonl」；字节级行切分（跨块多字节安全），直读磁盘不进 jsonlCache（真源唯一）；消费方：getChannelVersion / getMessagesSince（file-store.ts）、apps/api events 薄封装 |
| `runMigrations`, `MIGRATIONS`, `MigrationError`, `readSchemaVersion`, `backupDataArea`, `manifestPath` | migrations/（`./migrations` 子路径入口） | #572 数据区 schema 版本迁移框架（契约 §5 冻结方案 A）：版本标记 `data/manifest.json`（无 = v0；data 根与 FileStore 同口径 `STUDIO_DATA_DIR ?? <root>/data`）；四性质 = 幂等（isApplied 探针）/ 可重入（逐条 apply 后原子抬 manifest，中断续跑；写盘一律临时文件 + rename）/ 失败拒启（MigrationError 带备份路径 + 回滚指引，入口外层 catch exit(1)）/ 备份只含 data/ 与根级 *.json/*.jsonl（`.bak-<YYYYMMDD-HHMMSS>`，logs/worktrees/既有 .bak 不备）；骨架 = 空迁移 v0→v1，真实迁移随首次布局变更与契约修订同 commit；接入点 = apps/api index.ts 启动链 reconcileIndex() 之前 |
| `getSupportedFlags`, `warmCapabilityProbe`, `parseHelpFlags`, `resetCapabilityProbeCache` | capability-probe.ts（仅 /node 入口） | #565 能力探测：对声明 `capabilityProbe.helpArgs` 的 provider 跑 `<binary> <helpArgs>` 正则解析 flag 集合；进程内缓存 key = binary path + version（升级自然重探；which/version 定位结果同进程缓存）；探测失败/解析为空 **fail-open**（返回 undefined = 全量传参 + warn，失败同样入缓存不重试）；cli-scanner 扫描时 warm，spawn 前懒探测。配套注册表字段：ProviderDefinition.capabilityProbe / authProbe（三态判定见字段 JSDoc，未声明恒 unknown 不猜配置目录）、spawn.conditionalFlags（第一批仅 codex `--dangerously-bypass-hook-trust`，核心协议 flag 不标） |
| `classifyCliFailure`, `CliFailureClass`, `CliFailureCategory` | llm/failure-classifier.ts | #565 失败分类器（纯函数）：stderr/退出码已知特征 → 「去哪修」指引（auth/quota/rate_limit/cli_not_found/flag_unsupported），exit 127 直判 cli_not_found，无特征返回 null 走透传；**给人看**的产品语言指引，与 apps/api triage classifyFailure（内部路由策略）两套并存（2026-09-16 人审决策），模式表不共用；消费方 = studio-agent runner-lightweight 失败路径（ExecutionResult.failureClass + error `[category] guidance` 前缀） |
| `getProviderModels`, `parseModelList`, `resetModelProbeCache`, `ProviderModelsResult` | model-probe.ts（仅 /node 入口） | #574 动态模型发现：对声明 `listModels`（args + parser id：jsonModelSlugs=codex `debug models` 取 visibility==="list" 的 slug / jsonModelKeys=kimi `provider list --json` 取 models 键 / lines=opencode `models` 逐行）的 provider 跑探测；进程内缓存 key = binary path + version（升级自然重探，失败入缓存 null 不重试）；探测失败/超时/解析为空/未声明 → 注册表静态 `fallbackModels`（source=fallback），两者皆无 → undefined。探测命令逐 CLI 实测（2026-09-16，见 docs/plans/2026-09-provider-list-models.md）；claude 无列表命令不声明 listModels 仅 fallback。消费方 = cli-scanner detectProvider（随扫描探测，结果持久到 runtimes 记录） |

### 依赖关系

**上游（本目录依赖）**
- Node.js 内置模块: `fs`, `path`, `os`, `events`, `crypto`
- 第三方库: `yaml`

**下游（依赖本目录的模块）**
- `apps/api` 全套模块（daemon、middleware、modules、index、cli 等）广泛引用本目录的 CLI 框架、配置管理、事件总线及 file-store 类型

### 注意事项

- `llm/stream-json-parser.ts`（#564，2026-09-16）：`StreamEvent.parent_tool_use_id` 是子 agent（Task/subagent）子树事件标记——CLI 对子树事件回填 Task tool_use 的 id，父 run 顶层事件为 null/缺省；`extractResult`/`extractUsage` 据此过滤子树（子 error result 不误杀父 run、usage 不父子双计），assistant 文本与 tool_use 提取不做过滤
- 本包 `@types/node` 钉在 20.0.0（泛型前的 Buffer 类型），与 TS 5.7+ lib 的 `ArrayBufferView` 泛型不兼容——`handle.read(buffer)` / `Buffer.concat` 传 Buffer 会报类型错，解法 = 传参处 `as Uint8Array`（Buffer 运行时即 Uint8Array 子类，纯类型层适配，参照 jsonl-tail.ts）；根治 = 对齐 apps/api 的 ^20.12.7（未做，另议）

- CLI 命令注册表为全局单例，测试后需调用 `clearCommands()` 清理
- 配置优先级：环境变量 > `~/.studio/config.env` > 默认值，且仅当环境变量未设置时才加载 config.env
- 数据根：`studioDir()`（config/studio-dir）= `STUDIO_HOME` 或 `~/.studio`；config.env 与 `STUDIO_DATA_DIR ??=` 钉值均走它。dev 启动（scripts/dev/start.sh、apps/api dev script）默认 `STUDIO_HOME=~/.studio-dev`，prod systemd 显式指向生产数据根；非 production 指向缺省根时 `warnIfNonProdUsesProdRoot()` 落 warning（config/index.ts 初始化 + apps/api 入口显式调用，幂等）。**数据区布局唯一正本 = `docs/architecture/data-directory-contract.md`（#570，2026-09-16 冻结）；file-store.ts 头注释布局段已收敛为指针，布局变更须先修订契约**
- `studioDir()` 内含 vitest 内建模块双视图兼容层（resolveHomedir）：仓库测试对 `os.homedir` 有四种互不兼容的 mock 风格，该层保证迁移后的 SUT 在旧 mock 下仍被隔离；生产两视图恒等。测试收敛到 env 隔离后可删除（见 code-review follow-up）
- `FileStore` 使用 `flock` 目录锁（`mkdir` 原子操作）保障 claim 原子性；#169 起 withLock 持锁写 `owner.json`（pid/hostname/acquiredAt），EEXIST 时按双判据回收 stale 锁（同机死 pid / acquiredAt 或锁目录 mtime 超 30s；无属主裸锁——持锁方在写 owner.json 前被杀——按目录 mtime 超 2s 即回收，2026-08-26 flaky 修复），回收发 `lock.stale_reclaimed`、超时发 `lock.acquire_timeout`（均 warning，logger + eventBus，经 apps/api lock-events-bridge 走 dispatchMonitorAlerts 全管线）；同进程并发由进程内 per-lockDir mutex 排队不打到 mkdir
- `file-store-workunit.ts` 锁内复合原语（#170 起）：`commitSnapshot`/`commitRemoval`/`updateMetadata`/`createSnapshotGuarded`/`reconcileIndex`；#178 增 `refreshWorkUnitLease(wuId, expectedAssigneeId, expectedClaimedAt, timeoutAt)`——WU 租约心跳 fencing（claimedAt 代际令牌 + assigneeId 双比对），返回 'ok'/'lost'/'missing'，事件 data 走增量（reduce 合并语义）；#314 起心跳高频小写缓冲（每跳只做快速路 fencing + 内存 dirty 项），`flushWorkUnitLeases()` 锁内复核 fencing + status==='active' 后合并落盘（每 WU 一条增量事件 + 索引逐条 append），默认落盘窗口 `LEASE_FLUSH_INTERVAL_MS` 60s（≪5min TTL，构造注入 `leaseFlushIntervalMs: 0` = 每跳即落盘）。**#524 P1-2（2026-09-12）index.json 改 append-only JSONL**：upsert 追加快照行、remove 追加 `{id,deleted:true}` 墓碑行，读侧 fold（同 id 后行覆盖前行、墓碑删除、首现位置序，唯一口径 = 导出的 `parseWorkUnitIndexContent`，bench/脚本直读方共用）；claim/flushLeases/updateMetadata 全走 append 不再全量重写（写侧耗时不随 WU 数线性增长）；定期压实（`indexCompaction.{checkInterval:500,minLines:5000,deadRatio:0.3}`，测试可注入）锁内评估重写为 fold 结果；崩溃撕裂尾行读侧跳过、启动 `reconcileIndex` 按 events（正本）重建兜底；旧 pretty JSON 数组格式读侧兼容、首个写锁内一次性迁移；JSONL 缺 id 行/旧格式撕裂仍抛错（损坏不静默当空）
- `FileStore` 的 readJson/readJsonl/readdir 走模块级读穿缓存（stat mtime 校验 + 写/删精确失效，工单 26 A1）；缓存命中返回结构克隆，调用方 mutate 返回值不会污染缓存；#314 起 `getIndex` 也走该缓存（`readIndexForQuery` seam：基类缺省裸读、门面覆盖为缓存，保留撕裂抛错严格语义）；`readIndexFile` 保持无缓存（锁内跨进程正确性）——锁内读路径永不经过缓存 seam（docs/adr/2026-08-24-cache-seam-decision-rules.md 例外条款）；#406 起 filter 下推到克隆之前——seam 带 filter 参数，命中路径在共享缓存数组上按引用选行（`applyFilter` 唯一选行口径）只克隆命中子集，无 filter 全量克隆行为不变；filter 支持 id 字段后，点读约定 = `getIndex({ id })`（克隆 O(1)），禁止 `getIndex()` 全量取回再 `.find(id)`——例外只有需全量集做后续聚合/模糊匹配者（如 aggregateParentStatus 取 siblings、discord /stop 前缀兜底、skill-extraction 聚合过滤）
- `FileStore` 的 Requirement/Evolution 段共用泛型「序号分配型条目存储」实现（`SeqEntryStoreConfig`，工单 26 A2），新增同类存储应加配置而非复制段
- 事件总线支持通配符（`*`）模式订阅，Handler 异常不会影响其他监听器
- 级别常量为单一数据源，其他模块不应重复定义
- `constants/` 下各文件应保持无外部依赖（仅内部引用），便于前端复用
- `attestation.ts` 的 `deriveDisplayState()` 是 WU 展示状态唯一派生口径（F6 铁律，前后端共用）；#126（T4）起 `pending`（待确认人闸）为第七个看板列——按所有权状态原样透传；#280 起 pending 不计 needsHuman（pending 是「待确认」人闸，活未开干，与 in_review「活已干完等审查」/ done 缺 l3「活已干完等人工验收」语义不同），未知状态仍兜底 active
- `wu-display.ts` 是 WU 展示词表唯一出口（#358：7 份散装拷贝收口，经 /web 入口透出）：`WU_STATUS_LABELS`（含派生列 + failed/completed 原始状态）/ `WU_STATUS_COLORS` / `WU_TYPE_LABELS` + 阅览室文档词表（`LIBRARY_DOC_STATUS_*`）。#429 起 unassigned/active/in_review/done 收口为 #399 §8.3 正词（待领取/进行中/待验收/完成），RequirementChainPanel 改消费本模块不再是方言；新增 `formatChannelName` 为频道名渲染唯一出口（数据本身可能含前导 `#`，归一为恰好一个 `#` 前缀）。#472 起 ProjectPipeline 亦收口消费本模块；#472 颜色语义：pending「待确认」改中性色（与 in_review warning 分开），error 红只留 blocked/failed。已知有意方言未收（行为对齐另议）：mapUtils `DEP_STATUS_LABEL`（依赖图大白话文案）
- `utils/process-io.ts` 的 `execSh`（仅 /node 入口）：#171（#54 决议）起支持 `killProcessGroup`（detached spawn + `kill(-pid, SIGKILL)` 整组直杀，墙钟/静默/maxBuffer 三条杀路径同走；#68 实测 SIGTERM 杀不死孙进程）与 `silence` 静默看门狗（判据 = 距最后一次 stdout/stderr 输出间隔，warn 每段静默恰报一次、输出复位；超 killMs 杀并 reject）。未开选项的调用方行为不变
- `file-store.ts` 频道消息（#319，2026-08-24）：`appendMessage`/`softDeleteMessage` 走 per-channel `messages.lock` 互斥 + 写侧压实（每 500 写评估，≥5000 行且死行 ≥30% 时按 `mergeActiveRows` 唯一口径原子重写，只清死行）；`mergeActiveRows` 是「每 id 最新版、首现位置序、丢 deleted」归并的唯一实现（resolveActiveMessages/压实/getMessagesSince 共用）；§4.2 契约以消息 id 为锚（行号口径退役），`getMessagesSince` 锚点被压实抹除时保守返回全部活消息；`queryMessagesPage` 分页半下沉 + id 游标。**#524 P1-1（2026-09-12，#514 定案）消息读口尾部倒扫**：`readMessagesTail(channelId,{limit,match?})` 倒扫收集 limit 条活消息即停（去重/tombstone 口径复刻 mergeActiveRows——同 id 先见为准、deleted 占位不复活；**不按 createdAt 早停**，更新-append 使文件序≠时间序），返回新→旧 + `exhausted`（未凑满即扫到头 = 收集即全量）；`getMessageById(messageId, channelId?)` 有 channelId 走本频道倒扫直查（缺省保留全频道扇出兼容冷路径）；`queryMessages` limit 且无过滤、`queryMessagesPage` 首页（无 before）走倒扫快径——快径判据 = 窗口 createdAt **严格递减**（无更新副本/等 ts 撞车 → 文件序=时间序，页 = createdAt 精确后缀），违例回退全量路径保翻页链精确（单副本必被捕获；多副本交织病态窗口理论有界偏差，压实自愈）；首页热未穷举时 total 热部走 `countFileLines` 字节快扫原始行数（死行虚高，方向安全偏多）。**2026-09-16 channel 体检 B4/B5 热层倒扫收口**：before 锚在热层改 `scanHotToAnchor` 倒扫到锚再多收 limit+1 条即停（不再每页全量 resolve+sort+findIndex），锚穷举未命中直走冷链锚路径（hotIds/activeCount 为精确全量，免 resolve）；`queryMessages` 带过滤 + limit 走 `readMessagesTail` 谓词倒扫早停（无 limit 保持全量路径）。#576：`QueryOpts` 增 `before`（createdAt 严格 <，与 `since` 的 >= 互补），快径谓词与全量路径 filter 逐句同口径，供 WU 讨论串 limit 下推。两快径出页判据 = 窗口 createdAt 严格递减 + 锚前活跃消息 createdAt 全 > 锚点（防页内消息更新副本落尾部导致页错位），违例回退全量路径；测试 `__tests__/file-store-hot-scan.test.ts`（含扫描行数上界断言）。**#525 P2-4（2026-09-12，#517 项⑤）**：`MessagePageOpts.includeTotal` 开关、缺省 false——未开启时完全跳过 `countColdLines`/`countFileLines`、`total` 恒 0（消逐冷月字节扫纯浪费；前端不消费 total），要总数的调用方显式 `includeTotal: true`（HTTP 侧 `?includeTotal=true`），开启后精确口径不变
- `file-store.ts` 频道消息归档（#327，2026-08-25）：`archiveChannelMessages()` 超龄活消息热→冷（`archive/messages-YYYY-MM.jsonl` 按消息 createdAt 归月；计龄锚点 = 所属 WU `closedAt`（遗产回退 updatedAt / 悬空回退 createdAt / 非 closed 保留）或消息自身 createdAt，+30 天，经 `messageArchive.{maxAgeDays,now}` 注入）；`thawWorkUnitMessages(workUnitId)` reopen 解冻回热（锁内先 append 热、后原子重写冷，热侧同 id 不重复）；`queryMessagesPage` 穿透冷热边界（链式翻页：热新→旧接冷月新→旧，无 before 热页不足 limit 从冷补满——热全空首页直接出冷，跨冷热同 id 先见为准）且自候选 8（2026-09-08）起**冷链惰性分页**——逐月读、页凑满+hasMore（多收 1 条）即停，成本与页深成正比不与全历史成正比；`total` 统一为「热+冷原始行数」三分支同口径（原语义随分支漂移：无锚=全链总数/锚在冷=比锚点旧的数量；前端不消费 total），冷行数走 `countFileLines` 字节快扫不 parse/clone，thaw 残留行计入会虚高（方向安全）；`queryAllMessages`/`getMessageById`/`countMessages` 等其余查询面热只读；`queryAllMessages` 自 #330 起支持可选 `channelIds` 预过滤（readdir 后跳过集合外频道不读其文件，缺省全扫——observe 巡查只扫活跃 WU 频道）；`WorkUnitSnapshot.closedAt?` 是归档计龄锚点字段（可选，旧快照缺省兼容）
- `file-store.ts` 实例目录生命周期闭环（#363，2026-08-26）：`deleteState` 删 state.json 后判空删目录——为空才 rmdir，有 profile.json 或其他文件绝不碰（`agents/<id>/` 是 profile 与 state 共享 namespace）；`sweepEmptyAgentDirs()` 一次性存量清扫（同判空条件，幂等，挂载点 = apps/api index.ts 启动段）。背景：历史死实例只删 state.json 不删目录 → 空目录无界累积拖慢 `listStates`/`listProfiles`；terminated 实例回收统一归 apps/api instance-timeout-scan（跨角色每 5min）
- `file-store.ts` 目录型实体泛型（#362，2026-08-27）：Profile/RuntimeState/Channel 三段 CRUD 合并为 `DirEntityStoreConfig` 泛型实现（路径函数 + 过滤谓词 + 报错文案 + 删除口径为配置；对齐 SeqEntryStoreConfig 先例），新增同类存储应加配置而非复制段。漂移点统一口径：① 创建一律查重——重复建同 id 报错（Profile/Channel 异常路径从静默覆盖变报错；单进程语义，跨进程并发同 id 双建仍有竞态窗口）；② 更新一律自动补 updatedAt（RuntimeState 也补，类型新增可选 `updatedAt?`）。扁平目录 JSON 清单单点化 `FileStore.listJsonInDir(dir)`：消费方 mcp tool-store.listJsonFiles、capability scanAll；对旧实现的差异 = 串行改并发读、走 readdirCached 缓存、scanAll 不再有 mkdir 副作用
